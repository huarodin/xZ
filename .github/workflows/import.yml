name: worker

on:
  push:

jobs:
  formal:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        crystal: ["courtyard", "innocent", "league", "fail", "curious"]
        surgery: ["conclusion", "component", "retreat", "move"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar eC
